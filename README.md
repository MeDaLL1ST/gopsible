**Gopsible** — это легковесный, быстрый и простой аналог Ansible, написанный на **Go**.

Никакого Python, никаких зависимостей на целевых серверах, никакой сложной установки. Только один бинарный файл и ваши Bash-скрипты.

### 🔥 Почему Gopsible?

*   **Zero Dependencies:** Не нужно ставить Python или модули на сервера. Если там есть SSH и Bash — это работает.
*   **Single Binary:** Скомпилировал и скинул коллеге. Работает везде (Linux, Mac, Windows).
*   **Скорость:** Работает параллельно на сотнях серверов благодаря Go-рутинам.
*   **Простота:** Используйте привычный **Bash** для скриптов и **Go Templates** для переменных.
*   **Fail Fast:** Умная обработка ошибок. Упал один прод? Остановим деплой везде.
*   **Модульность:** Возможность разработки собственных модулей на языке **Golang**

---

## 📦 Установка

Скачайте последнюю версию из раздела [Releases](https://github.com/MeDaLL1ST/gopsible/releases) или соберите сами:

```bash
go install github.com/MeDaLL1ST/gopsible@latest
```

## 🚀 Быстрый старт
Создайте файл playbook.yaml:
```yaml
settings:
  fail_fast: true  # Если true: ошибка на одном хосте остановит всё

vars:
  app_port: "8080"
  deploy_dir: "/opt/myapp"

hosts:
  - name: "primary" # Имя для удобства
    address: "192.168.1.10:22"
    user: "root"
    key_path: "~/.ssh/id_rsa"

  - address: "192.168.1.11:22"
    user: "admin"
    password: "secretpassword"

tasks:
  - name: "Подготовка директории"
    type: "script"
    script: "mkdir -p {{ .deploy_dir }}"

  - name: "Загрузка конфига"
    type: "upload"
    src: "./config.json"
    dest: "{{ .deploy_dir }}/config.json"
    mode: "0644"

  - name: "Запуск сервиса"
    type: "script"
    script: |
      echo "Starting app on port {{ .app_port }}..."
      docker run -d -p {{ .app_port }}:80 my-image:latest
```
Запустите:
```bash
# Запуск дефолтного playbook.yaml
./gopsible
```

Или укажите конкретные файлы (можно несколько)
./gopsible db_setup.yaml app_deploy.yaml
## 📚 Документация
Типы задач (Tasks)
1. Script (Bash)
Выполняет команды на удаленном сервере. Поддерживает многострочные скрипты.
По умолчанию используется bash -e, поэтому при любой ошибке выполнение прерывается.
```yaml
- name: "Обновить пакеты"
  script: "apt-get update && apt-get upgrade -y"
  ignore_errors: true # Опционально: продолжить выполнение при ошибке
```
2. Upload (SFTP)
Загружает файлы с локальной машины на удаленную.
```yaml
- name: "Залить бинарник"
  type: "upload"
  src: "./bin/main"
  dest: "/usr/local/bin/main"
  mode: "0755" # Права доступа (chmod)
```
### Шаблонизация
Используйте синтаксис {{ .variable_name }}, чтобы подставлять значения из секции vars в скрипты и пути файлов.
### 🛠 Сборка проекта
```bash
# Сборка под текущую ОС
go build -o gopsible main.go

# Кросс-компиляция (пример для Linux)
GOOS=linux GOARCH=amd64 go build -o gopsible-linux main.go
```
### 🌍 Добавление в PATH (Глобальный запуск)

Чтобы запускать утилиту командой `gopsible` из любой папки (а не `./gopsible`), переместите её в системную директорию.

**Linux / macOS:**

```bash
# 1. Даем права на исполнение (если нужно)
chmod +x gopsible

# 2. Перемещаем в /usr/local/bin
sudo mv ./gopsible /usr/local/bin/gopsible

# Теперь можно запускать просто:
gopsible
```

Windows (PowerShell):
Запустите PowerShell от имени администратора и выполните:
```Powershell
# 1. Создаем папку для инструментов
$toolPath = "C:\Tools"
New-Item -ItemType Directory -Force -Path $toolPath

# 2. Перемещаем gopsible.exe туда (убедитесь, что вы находитесь в папке с файлом)
Move-Item -Path .\gopsible.exe -Destination $toolPath

# 3. Добавляем папку в PATH текущего пользователя
[Environment]::SetEnvironmentVariable(
    "Path",
    [Environment]::GetEnvironmentVariable("Path", "User") + ";$toolPath",
    "User"
)

# Перезапустите терминал, и команда gopsible станет доступна.
```